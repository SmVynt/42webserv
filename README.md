# Webserv - HTTP Server (School 42)

A custom HTTP/1 server implementation in C++17.

## Placeholder

## Build

```bash
make
```

## Usage

```bash
./webserv [config_file]
```

If no config file is specified, `config/default.conf` is used.
